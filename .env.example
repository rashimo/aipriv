# Gemini API Configuration (for legacy iserver.py)
GEMINI_API_KEY=your_api_key_here

# MCP Server Configuration (for mcp_server.py)
# These values can be overridden at connection time via the connect_to_client tool
#
# When using the proxy (recommended):
#   - proxy_relay.py listens on port 65432 for iclients
#   - proxy_relay.py listens on port 65433 for MCP server
#   - Set PRIVESC_CLIENT_PORT=65433 to connect to proxy
#
PRIVESC_CLIENT_HOST=127.0.0.1
PRIVESC_CLIENT_PORT=65433
